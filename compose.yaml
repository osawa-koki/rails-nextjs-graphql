services:
  server:
    build:
      context: ./server/
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    volumes:
      - ./server/:/app/
      - bundle:/usr/local/bundle

volumes:
  bundle:
